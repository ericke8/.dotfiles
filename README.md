# .dotfiles

### Prerequisites
```shell
$ sudo apt-get install stow
```

### Usage
```shell
$ git clone https://github.com/ericke8/.dotfiles.git ~/.dotfiles
$ cd ~/.dotfiles
$ stow vim git bash
```

### Unstow
```shell
$ stow -D vim git bash
```